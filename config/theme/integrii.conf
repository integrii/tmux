# integrii tmux theme
# Minimal theme config — custom colors, transparent status bar

# Pane borders
set -g pane-border-style "fg=#2A2A2A"
set -g pane-active-border-style "fg=#3399FF"

# Status bar — transparent background, white text
set -g status-style "bg=default,fg=#FFFFFF"
set -gu status-bg
